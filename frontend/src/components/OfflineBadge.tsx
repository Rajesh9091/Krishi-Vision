export default function OfflineBadge() {
  return (
    <div className="flex justify-center">
      <span className="rounded-full bg-black text-white text-xs font-semibold px-3 py-1 tracking-wide">
        ✈ Airplane-mode ready — runs fully on this device
      </span>
    </div>
  );
}
